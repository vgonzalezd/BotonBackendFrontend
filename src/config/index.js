/**
* index.js
* Archivo con los datos de configuracion de la aplicacion
*/
const server = require('./server');

  // Despues tenemos otras como informacion de autenticación, etc....
module.exports = {
  server: server
  //auth: auth
}
